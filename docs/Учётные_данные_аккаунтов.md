# Учётные данные аккаунтов SmartMedical

> Снимок БД: **17.05.2026** (локальная PostgreSQL `smartmedical`).  
> Вход на сайте — по полю **логин** (не email), через NextAuth Credentials.

---

## Администратор

| Поле | Значение |
|------|----------|
| ID | 21 |
| Логин | `admin` |
| Email | `admin@smartmedical.local` |
| Пароль | `Admin123!` |
| Роль | `ADMIN` |
| ФИО | Администратор |
| Телефон | +375290000000 |
| Дата регистрации | 2026-03-03 |

**Источник пароля:** `prisma/seeds.ts` (создаётся/обновляется при `npm run db:seed`).

### Дополнительно: экран входа в админ-панель

Перед работой в `/admin` показывается отдельный экран «Введите пароль для доступа» (не учётная запись в БД):

| Параметр | Значение |
|----------|----------|
| Пароль экрана | `admin123` |

**Источник:** `src/app/api/admin/auth/route.ts` (`ADMIN_GATE_PASSWORD`).

**Порядок входа в админку:**
1. Войти на сайт: логин `admin`, пароль `Admin123!`
2. Открыть `/admin` и ввести пароль экрана: `admin123`

---

## Пациенты (роль USER)

Пароли в БД хранятся как bcrypt-хеш; **исходные пароли для этих записей в репозитории не зафиксированы** (не совпадают с типовым набором тестовых паролей).

| ID | Логин | Email | ФИО | Телефон | Пароль | Дата регистрации |
|----|-------|-------|-----|---------|--------|------------------|
| 16 | `patient1` | patient1@example.com | Смирнов Пётр | +79001234567 | *неизвестен* | 2024-01-15 |
| 17 | `patient2` | patient2@example.com | Иванова Мария | +79007654321 | *неизвестен* | 2024-02-10 |
| 18 | `patient3` | patient3@example.com | Козлов Александр Викторович | +79001112233 | *неизвестен* | 2024-03-05 |
| 19 | `patient4` | patient4@example.com | Новикова Елена Сергеевна | +79002223344 | *неизвестен* | 2024-03-20 |
| 20 | `patient5` | patient5@example.com | Федоров Дмитрий Андреевич | +79003334455 | *неизвестен* | 2024-04-10 |

Чтобы задать известный пароль, например для `patient1`:

```sql
-- сгенерировать хеш: node -e "console.log(require('bcryptjs').hashSync('НовыйПароль123', 10))"
UPDATE patients SET password = '<bcrypt_hash>' WHERE login = 'patient1';
```

или сбросить через UI «Смена пароля» / повторную регистрацию тестового пользователя.

---

## Роли в системе (справочно)

В схеме Prisma определены: `USER`, `ADMIN`, `CHIEF_DOCTOR`, `OPERATOR`.  
В текущей БД есть только `USER` (5 записей) и `ADMIN` (1 запись). Учёток `CHIEF_DOCTOR` и `OPERATOR` нет.

---

## Сводка известных паролей

| Назначение | Логин / тип | Пароль |
|------------|-------------|--------|
| Админ (БД) | `admin` | `Admin123!` |
| Экран `/admin` | общий пароль доступа | `admin123` |
| patient1–patient5 | см. таблицу выше | не задокументированы |
